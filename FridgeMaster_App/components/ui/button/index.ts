import {Button as Native} from "./button.native";
import {Button as Web}  from "./button.web";
import { Platform } from "react-native";

export default function Button(props:any){
    if(Platform.OS === "web")
}